//package import
import { StyleSheet } from 'react-native';

//local import

const HomeStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  content: {
    padding: 16,
    flex: 1,
  },
});

export default HomeStyles;
