//package import
import { StyleSheet } from 'react-native';

//local import

const AuthFirstStyles = StyleSheet.create({
  container: {
    flex: 1,
    alignContent: 'center',
    alignSelf: 'center',
    alignItems: 'center',
  },
});

export default AuthFirstStyles;
