//package import
import { StyleSheet } from 'react-native';

//local import

const styles = StyleSheet.create({
  wrapModal: {
    flex: 1,
    height: '100%',
  },
  styleWrap: {
    borderRadius: 35 / 2,
  },
  styleContainer: {
    borderRadius: 35 / 2,
  },
});

export default styles;
